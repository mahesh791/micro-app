pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          git --version
          echo 'safkl' > versions
          git add . git && commit -m \"Added\"
        '''
      }
    }
  }
}
