pipeline {
  agent { dockerfile true }
  stages {
    stage('Test') {
      steps {
        sh '''
          git --version
          echo 'safkl' > versions
          git config --global user.email "deoremahesh791@gmail.com"
          git config --global user.name "Mahesh"
          git add . && git commit -m \"Added\"
        '''
      }
    }
  }
}
